[project]
name = "proto2"
version = "0.1.0"
description = "Continuous skill improvement from LangSmith + local coding-agent MCP capture"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "langsmith>=0.10.15",
]
